'use strict';

const utils = require('../app/utils');

const chai = require('chai');
const expect = chai.expect;

describe('Testing newsapi wrappers of bot', () => {
    it('TODO', () => {
    });
});